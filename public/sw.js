const safePath=value=>{const path=typeof value==='string'?value:'/';return path.startsWith('/')&&!path.startsWith('//')&&!path.includes('\\')&&!/[\u0000-\u001f\u007f]/.test(path)&&!/%(?:2f|5c)/i.test(path)?path:'/'};
self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{}const title=typeof data.title==='string'?data.title:'ME Media Intelligence',body=typeof data.body==='string'?data.body:'Neue Dashboard-Benachrichtigung',path=safePath(data.path),tag=typeof data.tag==='string'?data.tag:'wlx-alert';event.waitUntil(self.registration.showNotification(title,{body,icon:'/icons/app-192.png',badge:'/icons/app-192.png',tag,data:{path}}))});
self.addEventListener('notificationclick',event=>{event.notification.close();const path=safePath(event.notification.data&&event.notification.data.path);event.waitUntil((async()=>{const windows=await clients.matchAll({type:'window',includeUncontrolled:true});for(const client of windows)if('focus'in client){await client.focus();if('navigate'in client)await client.navigate(path);return}await clients.openWindow(path)})())});
