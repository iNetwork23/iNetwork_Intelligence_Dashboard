import{describe,expect,it}from'vitest';import{aggregateSourceRows,groupSources,mergeSourceWindows}from'./source-breakdown';import type{ReportRow}from'./portfolio';
const row=(source:string,sub:string,clicks:number,sois:number,campaign='0'):ReportRow=>({columns:[{column_type:'affiliate',id:'376',label:'Partner'},{column_type:'offer',id:'8',label:'Offer'},{column_type:'campaign',id:campaign,label:'N/A'},{column_type:'offer_url',id:'2766',label:'LP'},{column_type:'source_id',id:source,label:source},{column_type:'sub1',id:sub,label:sub}],reporting:{total_click:clicks,cv:sois}});
describe('source and sub-source breakdown',()=>{it('excludes Smartlink traffic and preserves absolute conversion bases',()=>{const rows=aggregateSourceRows([row('11000','news',100,12),row('11000','news',50,3),row('999','x',100,99,'2')]);expect(rows).toHaveLength(1);expect(rows[0]).toMatchObject({pathKey:'8|376|0|2766',sourceId:'11000',subSource:'news',metric:{clicks:150,sois:15,cvr:10}})});it('merges reporting windows and sorts sources and sub-sources by CR or SOIs',()=>{const merged=mergeSourceWindows([],[],[row('A','one',100,20),row('A','two',400,40),row('B','one',50,15)]);expect(groupSources(merged,'days30','cvr').map(x=>x.sourceId)).toEqual(['B','A']);expect(groupSources(merged,'days30','sois').map(x=>x.sourceId)).toEqual(['A','B']);expect(groupSources(merged,'days30','cvr')[1].metric).toMatchObject({clicks:500,sois:60,cvr:12})})});
