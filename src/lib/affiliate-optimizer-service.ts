import{unstable_cache}from'next/cache';import type{Period}from'./dashboard';import{getDashboard}from'./dashboard-service';import{analyzeAffiliateTraffic}from'./affiliate-optimizer';import{loadAffiliateConversionsFromCache,loadAffiliateSourceRowsFromCache}from'./cached-evaluations';import{analyzeLeadLatency}from'./lead-latency';import{mergeSourceWindows}from'./source-breakdown';
export async function getAffiliateOptimizations(){const[today,days7,days30]=await Promise.all([getDashboard('today'),getDashboard('7d'),getDashboard('30d')]);return analyzeAffiliateTraffic(today,days7,days30)}
const sourceWindow=(affiliateId:string,period:Period)=>unstable_cache(()=>loadAffiliateSourceRowsFromCache(period,affiliateId),['affiliate-source-cache',affiliateId,period],{revalidate:60,tags:[`affiliate-source-${affiliateId}`]})();
export async function getAffiliateSourceBreakdown(affiliateId:string){const[today,days7,days30]=await Promise.all([sourceWindow(affiliateId,'today'),sourceWindow(affiliateId,'7d'),sourceWindow(affiliateId,'30d')]);return mergeSourceWindows(today,days7,days30)}
export const getAffiliateLeadLatency=(affiliateId:string)=>unstable_cache(async()=>analyzeLeadLatency(await loadAffiliateConversionsFromCache(affiliateId,90)),['affiliate-lead-latency-cache',affiliateId,'90d'],{revalidate:900,tags:[`affiliate-latency-${affiliateId}`]})();
