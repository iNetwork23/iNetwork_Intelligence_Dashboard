import{describe,expect,it}from'vitest';import{existsSync,readFileSync}from'node:fs';import{join}from'node:path';
const root=join(process.cwd(),'src'),read=(path:string)=>readFileSync(join(root,path),'utf8');
describe('instant interaction performance contract',()=>{it('renders a global route-transition skeleton before server data resolves',()=>{const path=join(root,'app/loading.tsx');expect(existsSync(path)).toBe(true);expect(readFileSync(path,'utf8')).toContain('aria-live="polite"')});it('renders an immediate route-transition skeleton',()=>{const path=join(root,'app/affiliates/loading.tsx');expect(existsSync(path)).toBe(true);expect(readFileSync(path,'utf8')).toContain('aria-live="polite"')});it('marks route links pending on pointer-down before server data resolves',()=>{const path=join(root,'app/affiliates/InstantLink.tsx');expect(existsSync(path)).toBe(true);expect(readFileSync(path,'utf8')).toContain('onPointerDown')});it('keeps the pending anchor clickable so pointer-down feedback cannot cancel the following click',()=>{const css=read('app/globals.css');expect(css).not.toMatch(/\.routePending\{[^}]*pointer-events:none/);expect(css).toMatch(/\.instantLinkFeedback\{[^}]*pointer-events:none/)});it('gives every lazy disclosure one shared chevron that rotates when open',()=>{const component=read('app/affiliates/LazyDetails.tsx'),css=read('app/globals.css');expect(component).toContain('expandableDetails');expect(component).toContain('expandChevron');expect(css).toMatch(/details\.expandableDetails\[open\][^}]*\.expandChevron/);expect(css).toContain('rotate(90deg)');expect(css).toMatch(/\.expandChevron\{[^}]*transition:transform 150ms/)});it('does not duplicate the shared chevron inside disclosure labels',()=>{for(const path of['app/affiliates/page.tsx','app/affiliates/SourceBreakdown.tsx','app/affiliates/AffiliateSmartlinks.tsx'])expect(read(path)).not.toMatch(/(?:Details|Entscheidungen) ›/)});it('visually connects every open parent to its disclosure body and gives summaries hover feedback',()=>{const css=read('app/globals.css');expect(css).toMatch(/details\.expandableDetails\[open\]\{[^}]*box-shadow:inset 3px 0/);expect(css).toMatch(/details\.expandableDetails\[open\]>.expandableSummary\{[^}]*background/);expect(css).toMatch(/details\.expandableDetails>.expandableSummary:hover\{[^}]*background/);expect(css).toMatch(/details\.expandableDetails\[open\]>.expandableSummary~\*\{[^}]*border-left:[^;}]+;[^}]*background/)});it('mounts heavy drilldown bodies only after their panel opens and keeps them cached',()=>{const path=join(root,'app/affiliates/LazyDetails.tsx');expect(existsSync(path)).toBe(true);const source=readFileSync(path,'utf8');expect(source).toContain('requestAnimationFrame');expect(source).toContain('mounted')});it('uses browser rendering containment for long rows',()=>{expect(read('app/globals.css')).toContain('content-visibility:auto')})});
