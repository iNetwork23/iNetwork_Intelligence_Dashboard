import{describe,expect,it}from'vitest';import{filterCampaignOptions}from'./campaign-picker';const campaigns=[{network_campaign_id:2,campaign_name:'Trafficpartner - POP - ALL',campaign_status:'active'},{network_campaign_id:146,campaign_name:'Global - TrafficCompany',campaign_status:'active'},{network_campaign_id:169,campaign_name:'WLX Offer 57',campaign_status:'inactive'}];describe('searchable campaign dropdown',()=>{it('shows the complete directory when search is empty',()=>expect(filterCampaignOptions(campaigns,'')).toEqual(campaigns));it('filters by ID, name, and status',()=>{expect(filterCampaignOptions(campaigns,'146')).toEqual([campaigns[1]]);expect(filterCampaignOptions(campaigns,'trafficcompany')).toEqual([campaigns[1]]);expect(filterCampaignOptions(campaigns,'inactive')).toEqual([campaigns[2]])})});
