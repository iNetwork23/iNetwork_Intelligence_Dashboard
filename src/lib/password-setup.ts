export function validatePasswordSetup(value:unknown){if(!value||typeof value!=='object'||Array.isArray(value))throw new Error('Ungültige Anfrage');const input=value as Record<string,unknown>,accessToken=String(input.access_token||''),password=String(input.password||''),confirm=String(input.confirm||'');if(accessToken.length<32||accessToken.length>4096)throw new Error('Ungültiges Token');if(password!==confirm||password.length<14||password.length>128||!/[A-Z]/.test(password)||!/[a-z]/.test(password)||!/[0-9]/.test(password)||!/[\W_]/.test(password))throw new Error('Passwortanforderungen nicht erfüllt');return{accessToken,password};}
