import{readFileSync}from'node:fs';import{join}from'node:path';import{describe,expect,it}from'vitest';
describe('partner messaging specification',()=>{const docs=readFileSync(join(process.cwd(),'docs/PARTNER-MESSAGING-SPEC.md'),'utf8');it('uses canonical isolated traffic modes and fails closed on unknown',()=>{for(const mode of ['tracked_smartlink','tracked_direct','clickless_api','unknown'])expect(docs).toContain(mode);expect(docs).not.toContain("trafficMode: 'tracked' | 'api'")});it('defines deterministic provider safety limits',()=>{for(const term of ['16 KiB','4.096 Zeichen','Draft-TTL 15 Minuten','zehn Previews','drei Send-Claims','ohne `parse_mode`','weder in URLs noch in Logs'])expect(docs).toContain(term)})});
