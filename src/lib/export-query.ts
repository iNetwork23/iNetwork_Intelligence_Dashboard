export type ExportFilters={from?:string|null;to?:string|null;affiliate?:string;offer?:string;campaign?:string;source?:string;sub_source?:string};
type QueryResult={data:unknown[]|null;error:{message:string}|null};
export interface ExportQuery extends PromiseLike<QueryResult>{select(columns:string):ExportQuery;order(column:string,options:{ascending:boolean}):ExportQuery;limit(count:number):ExportQuery;gte(column:string,value:string):ExportQuery;lte(column:string,value:string):ExportQuery;eq(column:string,value:string):ExportQuery;in(column:string,values:string[]):ExportQuery}
type Client={from(table:string):ExportQuery};
export const EXPORT_COLUMNS='metric_date,affiliate_id,affiliate_name,offer_id,offer_name,campaign_id,campaign_name,offer_url_id,offer_url_name,source_id,sub_source,clicks,sois,first_sales,rebills,coin_spend,payout,revenue,profit';
export function buildDailyMetricsExportQuery(client:Client,filters:ExportFilters,scopes?:Partial<Record<'affiliate'|'offer'|'campaign'|'source'|'sub_source',string[]>>){let query=client.from('daily_metrics').select(EXPORT_COLUMNS).order('metric_date',{ascending:false}).limit(10_000);if(filters.from)query=query.gte('metric_date',filters.from);if(filters.to)query=query.lte('metric_date',filters.to);const columns={affiliate:'affiliate_id',offer:'offer_id',campaign:'campaign_id',source:'source_id',sub_source:'sub_source'} as const;for(const key of Object.keys(columns) as Array<keyof typeof columns>){const allowed=scopes?.[key];if(allowed?.length)query=query.in(columns[key],allowed);const requested=filters[key];if(requested)query=query.eq(columns[key],requested)}return query;}