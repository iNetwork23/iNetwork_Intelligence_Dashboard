import Link from 'next/link';import {redirect} from 'next/navigation';import {currentUser} from '@/lib/session';import AutomationDashboard from './AutomationDashboard';
export const dynamic='force-dynamic';
export default async function AutomationPage(){if(!await currentUser())redirect('/login');return <main className="dashboard automationMonitor"><header className="topbar"><div><div className="eyebrow">ME MEDIA · LIVE OPERATIONS</div><h1>Auto-Rotation <span>Live</span></h1><p>Was geprüft, entschieden und tatsächlich in Everflow rotiert wurde.</p></div><div className="topActions"><Link className="logout" href="/affiliates">Affiliate Optimizer</Link><Link className="logout" href="/smartlinks">Smartlink Intelligence</Link><Link className="logout" href="/">Account Monitor</Link><form action="/api/auth/logout" method="post"><button className="logout">Abmelden</button></form></div></header><AutomationDashboard/></main>}
