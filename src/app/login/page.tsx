import {redirect} from 'next/navigation';
import {currentUser} from '@/lib/session';
import LanguageToggle from '@/app/components/LanguageToggle';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect('/');const {error}=await searchParams;return <main className="loginShell"><div className="loginLanguage"><LanguageToggle/></div><section className="loginCard"><div className="eyebrow">ME MEDIA · PERFORMANCE MONITOR</div><h1>Everflow Dashboard</h1><p>Geschützter Zugriff für Mitarbeitende und Partner.</p>{error&&<div className="loginError">E-Mail, Benutzername oder Passwort ist falsch.</div>}<form action="/api/auth/login" method="post"><label htmlFor="username">E-Mail oder Benutzername</label><input id="username" name="username" type="text" autoComplete="username" autoCapitalize="none" spellCheck={false} required autoFocus/><label htmlFor="password">Passwort</label><input id="password" name="password" type="password" autoComplete="current-password" required/><button>Anmelden</button></form><div className="security"><span/>HttpOnly-Session · 30 Min. inaktiv · max. 12 Std.</div></section><aside className="loginAside"><div className="offerTag">ROLLENBASIERTER ZUGRIFF</div><h2>Profit statt Datenrauschen.</h2><p>Sichtbarkeit und Partnerdaten werden serverseitig nach individuellen Rechten und Scopes begrenzt.</p><dl><div><dt>Session</dt><dd>Widerrufbar</dd></div><div><dt>Daten</dt><dd>Scope-gefiltert</dd></div><div><dt>Schutz</dt><dd>Deny by default</dd></div></dl></aside></main>}
