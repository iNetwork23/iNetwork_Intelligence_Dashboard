import { redirect } from 'next/navigation';
import { currentUser } from '@/lib/session';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect('/');const {error}=await searchParams;return <main className="loginShell"><section className="loginCard"><div className="eyebrow">ME MEDIA · PERFORMANCE MONITOR</div><h1>WLX Dashboard</h1><p>Geschützter Zugriff auf Offer 57 und Smartlink #169.</p>{error&&<div className="loginError">Benutzername oder Passwort ist falsch.</div>}<form action="/api/auth/login" method="post"><label htmlFor="username">Benutzername</label><input id="username" name="username" autoComplete="username" required autoFocus/><label htmlFor="password">Passwort</label><input id="password" name="password" type="password" autoComplete="current-password" required/><button>Anmelden</button></form><div className="security"><span/>HttpOnly-Session · 12 Stunden · HTTPS</div></section><aside className="loginAside"><div className="offerTag">OFFER 57</div><h2>Profit statt Datenrauschen.</h2><p>Aktive Rotation, First-Sales, Rebills und wirtschaftliche Qualität auf einen Blick.</p><dl><div><dt>Partner</dt><dd>WLX #32</dd></div><div><dt>Smartlink</dt><dd>#169</dd></div><div><dt>Modus</dt><dd>Read only</dd></div></dl></aside></main>}
