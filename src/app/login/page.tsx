import { redirect } from 'next/navigation';
import { currentUser } from '@/lib/session';
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){if(await currentUser())redirect('/');const {error}=await searchParams;return <main className="loginShell"><section className="loginCard"><div className="eyebrow">ME MEDIA · PERFORMANCE MONITOR</div><h1>Everflow Dashboard</h1><p>Geschützter Zugriff auf den gesamten Everflow-Account.</p>{error&&<div className="loginError">Benutzername oder Passwort ist falsch.</div>}<form action="/api/auth/login" method="post"><label htmlFor="username">Benutzername</label><input id="username" name="username" defaultValue="ergin" autoComplete="username" required autoFocus/><label htmlFor="password">Passwort</label><input id="password" name="password" type="password" autoComplete="current-password" required/><button>Anmelden</button></form><div className="security"><span/>HttpOnly-Session · 12 Stunden · HTTPS</div></section><aside className="loginAside"><div className="offerTag">GESAMTER ACCOUNT</div><h2>Profit statt Datenrauschen.</h2><p>Alle Offers, Partner, Smartlinks, Direktpfade, Landingpages, First-Sales und Rebills in einer Oberfläche.</p><dl><div><dt>Scope</dt><dd>Accountweit</dd></div><div><dt>Daten</dt><dd>Live</dd></div><div><dt>Modus</dt><dd>Read only</dd></div></dl></aside></main>}
