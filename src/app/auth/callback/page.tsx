'use client';
import {useEffect,useState} from 'react';
export default function PasswordSetup(){const[token,setToken]=useState(''),[message,setMessage]=useState('');useEffect(()=>{const hash=new URLSearchParams(location.hash.slice(1));setToken(hash.get('access_token')||'');history.replaceState(null,'',location.pathname);},[]);async function submit(formData:FormData){setMessage('');const password=String(formData.get('password')||''),confirm=String(formData.get('confirm')||'');const response=await fetch('/api/auth/password-setup',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({access_token:token,password,confirm})});setMessage(response.ok?'Passwort gesetzt. Sie können sich jetzt anmelden.':'Passwort konnte nicht gesetzt werden. Link prüfen oder neuen Reset anfordern.');if(response.ok)setToken('');}return <main className="loginShell"><section className="loginCard"><div className="eyebrow">SICHERER ZUGANG</div><h1>Passwort einrichten</h1>{!token&&<p>Der Link enthält kein gültiges Zugriffstoken.</p>}<form action={submit}><label htmlFor="password">Neues Passwort</label><input id="password" name="password" type="password" minLength={14} required/><label htmlFor="confirm">Passwort bestätigen</label><input id="confirm" name="confirm" type="password" minLength={14} required/><button disabled={!token}>Passwort speichern</button></form>{message&&<p role="status">{message}</p>}</section></main>}
