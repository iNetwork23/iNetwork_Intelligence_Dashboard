'use client';
import Link from'next/link';import{useState,type ComponentProps,type PointerEvent,type MouseEvent}from'react';
type Props=ComponentProps<typeof Link>;
export default function InstantLink({children,className='',onPointerDown,onClick,...props}:Props){const[pending,setPending]=useState(false);const start=(event:PointerEvent<HTMLAnchorElement>)=>{onPointerDown?.(event);if(!event.defaultPrevented&&event.button===0&&!event.metaKey&&!event.ctrlKey&&!event.shiftKey&&!event.altKey)setPending(true)},click=(event:MouseEvent<HTMLAnchorElement>)=>{onClick?.(event);if(event.defaultPrevented)setPending(false)};return <Link {...props} prefetch={props.prefetch??true} onPointerDown={start} onClick={click} aria-busy={pending} className={`${typeof className==='string'?className:''}${pending?' routePending':''}`.trim()}>{children}{pending&&<span className="instantLinkFeedback" aria-live="polite"><i/>Lädt …</span>}</Link>}
