'use client';
import LazyDetails from'./LazyDetails';import CopyValue from'./CopyValue';import SourcePeriodControls from'./SourcePeriodControls';import{useDeferredValue,useMemo,useState}from'react';import{groupSources,type BreakdownSort,type BreakdownWindow,type ConversionMetric,type SourceBreakdownRow,type TrafficAction}from'../../lib/source-breakdown';import type{ResolvedSourcePeriod}from'../../lib/source-period';
const num=(n:number)=>new Intl.NumberFormat('de-DE').format(n),pct=(n:number)=>`${n.toFixed(1).replace('.',',')} %`,eur=(n:number)=>new Intl.NumberFormat('de-DE',{style:'currency',currency:'EUR'}).format(n),metric=(m:ConversionMetric,api=false)=>api?`${num(m.sois)} SOIs · ${num(m.firstSales)} First-Sales · ${pct(m.firstSaleRate)}`:m.clicks?`${pct(m.cvr)} CVR · ${num(m.sois)} SOIs aus ${num(m.clicks)} Klicks`:`CVR nicht berechenbar · ${num(m.sois)} SOIs · keine Klicks`,actionClass=(action:TrafficAction|string)=>action.includes('SKALIEREN')?'positive':action.includes('ABSCHALTEN')?'critical':'neutral';
export default function SourceBreakdown({rows,apiMode=false,rangeLabel='Gewählter Zeitraum',sourcePeriod,initialSort='sois'}:{rows:SourceBreakdownRow[];apiMode?:boolean;rangeLabel?:string;sourcePeriod?:ResolvedSourcePeriod;initialSort?:BreakdownSort}){const reportWindow:BreakdownWindow='days30',[sort,setSort]=useState<BreakdownSort>(initialSort),deferredSort=useDeferredValue(sort),groups=useMemo(()=>groupSources(rows,reportWindow,deferredSort),[rows,deferredSort]),refreshing=sort!==deferredSort,chooseSort=(next:BreakdownSort)=>{setSort(next);if(typeof globalThis.window!=='undefined'){const params=new URLSearchParams(globalThis.window.location.search);params.set('sourceSort',next);globalThis.history.replaceState(null,'',`${globalThis.window.location.pathname}?${params}${globalThis.window.location.hash}`)}};return <section className={`sourceBreakdown${refreshing?' isRefreshing':''}`} aria-busy={refreshing}><header><div><span>{apiMode?'ADV1-/ADV2-QUELLENAUSWERTUNG':'SOURCE-/SUB-SOURCE-ENTSCHEIDUNGEN'}</span><b>{apiMode?'Clickless-Auswertung nach Quelle und Unterquelle':'Empfehlung immer auf der tiefsten verfügbaren Ebene'}</b></div><div className="breakdownControls">{sourcePeriod?<SourcePeriodControls period={sourcePeriod}/>:<div><small>{rangeLabel} · Europe/Berlin</small></div>}<div><small>Sortieren:</small><button type="button" className={sort==='sois'?'active':''} onClick={()=>chooseSort('sois')}>SOIs</button>{!apiMode&&<button type="button" className={sort==='cvr'?'active':''} onClick={()=>chooseSort('cvr')}>CR</button>}</div></div></header>{groups.length?<div className="sourceGroups">{groups.map(group=><LazyDetails key={group.sourceId} summary={<><span><CopyValue label={apiMode?'ADV1':'Source'} value={group.sourceId}/><small>{group.hasSubSources?`${group.leaves.length} tiefste Sub-Sources`:'Keine Sub-Source · Source-Fallback'}</small></span>{!apiMode&&<span className={`trafficAction ${actionClass(group.action)}`}><b>{group.action}</b><small>{group.actionReason}</small></span>}<strong>{metric(group.metric,apiMode)}<small className={group.metric.profit>=0?'up':'down'}>{eur(group.metric.profit)} Profit</small></strong><i>{apiMode?'Details':'Entscheidungen'}</i></>}><div><div className="subSourceHead decision"><span>{apiMode?'ADV2 · Unterquelle':'Tiefste Einheit'}</span><span>{apiMode?'Ereignisse':'Empfehlung'}</span><span>{apiMode?'SOIs · First-Sales · Rate':'CR · SOIs / Klicks'}</span><span>Umsatz – Payout = Profit</span></div>{group.leaves.map(leaf=><article className="trafficLeaf" key={`${leaf.sourceId}|${leaf.subSource||'source'}`}><span>{leaf.subSource?<CopyValue label={apiMode?'ADV2':'Sub1'} value={leaf.subSource}/>:<CopyValue label={apiMode?'ADV1':'Source'} value={leaf.sourceId}/>}<small>{leaf.subSource?`${apiMode?'ADV2':'Sub-Source'} unter ${leaf.sourceId}`:`Bewertung direkt auf ${apiMode?'ADV1':'Source'}-Ebene`}</small></span>{apiMode&&<span className="trafficAction neutral"><b>{num(leaf.metric.rebills)} Rebills</b><small>{num(leaf.metric.coinSpend)} Coin-Spend · {eur(leaf.metric.profitPerSoi)} Profit/SOI</small></span>}{!apiMode&&<span className={`trafficAction ${actionClass(leaf.assessment.action)}`}><b>{leaf.assessment.action}</b><small>{leaf.assessment.reason}</small></span>}<strong>{metric(leaf.metric,apiMode)}</strong><span className="economics"><small>{eur(leaf.metric.revenue)} Umsatz – {eur(leaf.metric.payout)} SOI-Vergütung</small>{leaf.metric.revenue>0&&leaf.metric.clicks===0&&leaf.metric.sois===0&&<em>Nachlaufender Umsatz ohne neuen Traffic</em>}<b className={leaf.metric.profit>=0?'up':'down'}>{eur(leaf.metric.profit)} Profit</b></span></article>)}</div></LazyDetails>)}</div>:<p className="noSourceData">Für diese Offer-URL liegen im gewählten Fenster keine Source-/Sub-Source-Daten vor.</p>}<footer className="decisionThresholds">{apiMode?<>API · clickless: keine klickbasierten Stop-/Scale-Regeln. Primär zählen SOIs, First-Sales, Rebills, Profit und Profit je SOI.</>:<>Entscheidungsreif ab <b>100 Klicks</b> oder <b>20 SOIs</b>. Skalieren erst ab 20 SOIs und positivem Profit; Abschalten bei reifem negativem Profit oder 100 Klicks ohne SOI.</>}</footer></section>}
